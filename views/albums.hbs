<div class="artist-search">
    {{#each dataAlbums }}
    <div class="artist">
        <h1>{{ name }}</h1>
        <img src="{{ images.0.url }}" />
        <a href ="/tracks/{{ id }}" target="_blank">View tracks</a>
    </div>
    {{/each}}
</div>